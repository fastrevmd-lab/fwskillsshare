# Skill subtree instructions

- Keep each selected `SKILL.md` concise, triggerable, and internally consistent.
- Read and update directly referenced resources when behavior changes.
- Preserve shared firewall-schema byte identity across parser skills.
- Use verified vendor syntax and current primary standards sources; do not
  overclaim device, framework, or environment compliance.
- Fixtures must be synthetic and secret-free. Any device write example requires
  approval, dry-run/diff, rollback, and verification steps.
